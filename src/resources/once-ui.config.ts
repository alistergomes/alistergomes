// src/resources/once-ui.config.ts

// ... keep imports ...

const baseURL: string = "https://alister-gomes-portfolio.vercel.app"; // Update this

// ... keep routes ...

const display: DisplayConfig = {
  location: true,
  time: true,
  themeSwitcher: true,
};

// REMOVED the sample protected route
const protectedRoutes: ProtectedRoutesConfig = {}; 

// ... keep fonts and style ...
